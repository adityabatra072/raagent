<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext HTTP by default, which silently breaks the one
  case this app is built for: pointing it at a model endpoint or MCP server
  running on your own machine. Those are plain http:// on localhost or a
  private LAN address, and the failure surfaced only as "Network request
  failed" in the Tools screen.

  Public traffic stays HTTPS-only; cleartext is permitted exactly for
  loopback, the emulator's host alias, and RFC1918 ranges.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <!-- Android emulator alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
</network-security-config>
