{
  "name": "@raagent/eval",
  "version": "0.1.0",
  "description": "YAML scenario eval harness for the raagent agent-core against local (rcli/llama-server) or remote OpenAI-compatible model backends.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "@raagent/agent-core": "*",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
